org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Adres produkcyjnej strony (z https:// i / na końcu).
# Używany przy budowaniu APK na GitHub Actions, jeśli nie podasz URL w „Run workflow”.
# Przykład: API_BASE_URL=https://twoja-akademia.vercel.app/
API_BASE_URL=https://ZMIEŃ-NA-SWOJĄ-DOMENĘ/
