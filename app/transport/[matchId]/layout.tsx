import { TransportRouteShell } from "@/components/transport-route-shell";

export default function TransportMatchLayout({ children }: { children: React.ReactNode }) {
  return <TransportRouteShell>{children}</TransportRouteShell>;
}
